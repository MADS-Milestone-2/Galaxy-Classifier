These are all the auxiliary files needed for the notebooks to run. Please keep all these files together in one place to ensure correct functionality.

requirements.txt include all imports that need to be pip installed. Simply type pip install -r requirements.txt in a terminal to quickly install dependencies before attempting to train/evaluate/generate